<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="course.javaweb.dao.ContentDao">
    <insert id="addContent" parameterType="course.javaweb.model.Content" useGeneratedKeys="true" keyProperty="id">
    insert into content(price,title,icon,abstract,text)
             values(#{price},#{title},#{icon},#{summary},#{text})
    </insert>
    <resultMap type="course.javaweb.model.Content" id="AllContent">
        <id column="id" property="id" />
        <result column="price" property="price" />
        <result column="title" property="title" />

        <result column="icon" property="icon" />
        <result column="abstract" property="summary" />
        <result column="text" property="text" />
    </resultMap>
    <select id="GetAllContent" resultMap="AllContent">
    SELECT  * from content
    </select>
</mapper>